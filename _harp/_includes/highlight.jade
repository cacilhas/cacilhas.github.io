- var count = 0

each yearDir, yearSlug in public.montegasppa
  if yearDir
    each monthDir, monthSlug in yearDir
      if monthDir._data
        each page, slug in monthDir._data
          if count < 3
            if (!page.hidden)
              - count++
              - var url = "montegasppa/" + yearSlug + "/" + monthSlug + "/" + slug + ".html"

              a(class=["col-md-4", "mg-nou"] href=url role="button")
                div(class=["panel", "panel-default"])
                  div.panel-heading
                    h3.mg-title Reflexões… :: !{page.title}
                  div.panel-body
                    p.lead!= page.content

          else
            - break

- count = 0

each yearDir, yearSlug in public.kodumaro
  if yearDir
    each monthDir, monthSlug in yearDir
      if monthDir._data
        each page, slug in monthDir._data
          if count < 3
            if (!page.hidden)
              - count++
              - var url = "kodumaro/" + yearSlug + "/" + monthSlug + "/" + slug + ".html"

              a(class=["col-md-4", "mg-nou"] href=url role="button")
                div(class=["panel", "panel-default"])
                  div.panel-heading
                    h3.mg-title Kodumaro :: !{page.title}
                  div.panel-body
                    p.lead!= page.content

          else
            - break
